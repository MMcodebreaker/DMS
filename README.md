# Laravel 11 + Inertia.js + Vuetify Starter


```bash
cd LaravelStarter && \
composer update && \
cp .env.example .env && \
npm install && \
php artisan key:generate && \
php artisan migrate:fresh
```

```bash
php artisan serve
npm run dev
```
